server:
  port: 8762

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka
  instance:
    hostname: localhost
#    prefer-ip-address: true

#当前服务名称
spring:
  application:
    name: eureka-client