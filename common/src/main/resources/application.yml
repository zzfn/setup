server:
  port: 8765

eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.ip-address}:8761/eureka
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true

#当前服务名称
spring:
  application:
    name: eureka-common